import domPointLabel from './domPointLabel';
import domPointImage from './domPointImage';
import tooltip from './tooltip';

const components = {
  domPointLabel,
  domPointImage,
  tooltip,
};

module.exports = components;
